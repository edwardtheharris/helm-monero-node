apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monero.fullname" . }}-test-connection"
  labels:
    {{- include "monero.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monero.fullname" . }}:{{ .Values.service.main.ports.main.port }}']
  restartPolicy: Never
